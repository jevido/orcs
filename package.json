{
  "name": "orcs-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "test": "bun test packages/orcs/tests",
    "dev": "cd example && bun --watch server.js",
    "publish:framework": "cd packages/orcs && bun publish"
  }
}
